@AGENTS.md

# 프로젝트 작업 규칙

## Git 작업 및 커밋 규칙 (필수)

작업 완료 시 또는 사용자의 커밋/배포/완료 요청이 있을 때 **묻지 말고** 아래 순서대로 자동 수행한다.

### 1. 커밋 메시지 한글 작성
- 커밋 메시지는 **반드시 한글**로 번역/작성한다.
- 접두사 타입(`feat`, `fix`, `chore`, `docs`, `style`, `refactor` 등)만 영문으로 작성하고 제목과 내용은 한글로 작성한다.
- 제목은 50자 내외로 핵심을 요약하며, 세부 변경 사항이 있을 경우 빈 줄 뒤에 본문을 기재한다.
- 커밋 명령:
  ```bash
  git add -A
  git commit -m "<타입>: <한글 요약>"
  ```

### 2. 풀(Pull) 및 푸시(Push) 동기화
- 원격 저장소(`origin`)가 연결되어 있는 경우, 반드시 최신 상태를 가져오고 푸시한다:
  ```bash
  git pull --rebase origin main
  git push origin main
  ```
- 원격 저장소가 설정되지 않은 경우 커밋까지만 완료하고 사용자에게 알린다.
- `pull --rebase` 중 충돌이 발생하면 임의로 해결하지 말고 즉시 중단 후 사용자에게 알린다.

## `._*` 파일 처리 규칙 (필수)

- 이 프로젝트는 exFAT 외장하드에 있어 macOS가 `._파일명` 형태의 AppleDouble 메타데이터 파일을 자동 생성한다.
- `._*` 파일은 expo-router 라우트 스캔, git 팩 인덱스 등을 깨뜨리므로 **발견 즉시 전부 삭제**한다.
- `.claude/settings.json`의 SessionStart / UserPromptSubmit / PostToolUse 훅이 자동으로 삭제한다. 훅이 놓친 경우 아래 명령으로 수동 정리한다:
  ```bash
  find . .. -name '._*' -type f -delete
  ```
- `._*` 파일은 절대 커밋하지 않는다 (`.gitignore`에 등록됨).

## 개발 서버 규칙 (필수)

### 1. 서버는 항상 떠 있어야 한다

**사용자가 직접 끈 게 아니라면 서버는 언제나 실행 중이어야 한다.**

- 작업 도중 서버(포트 8081)가 꺼진 것을 확인하면 **묻지 말고 즉시 다시 띄운다**.
  ```bash
  ./scripts/ensure-server.sh   # 꺼져 있을 때만 켠다 (이미 떠 있으면 아무것도 안 함)
  ```
- **검증하려고 서버를 껐다면 반드시 다시 켜고 작업을 끝낸다.**
  빌드 확인·렌더 확인 같은 이유로 `stop.sh`나 `kill`을 썼다면,
  응답을 보내기 전에 서버가 다시 떠 있는지 확인한다. 꺼진 채로 두지 않는다.
- 3중 안전장치가 걸려 있다:
  1. `start.sh` / `start.bat` — 서버가 비정상 종료되면 3초 뒤 자동 재실행.
     10초 안에 5번 연속 죽으면 무한 재시작을 막기 위해 멈추고 오류를 보여준다.
  2. `scripts/ensure-server.sh` — 꺼져 있으면 켜고, 떠 있으면 그대로 둔다.
     중복 실행을 막기 위해 포트와 expo 프로세스를 모두 확인한다.
  3. `.claude/settings.json` 훅 — 세션 시작 / 사용자 입력 / 작업 종료 시점마다
     위 스크립트를 돌려 서버가 살아 있는지 확인한다.
- 사용자가 Ctrl+C로 직접 멈춘 경우 `start.sh`는 재시작하지 않는다.

### 2. 자동 새로고침(Fast Refresh)이 안 될 때
- 원인은 거의 항상 `._*` 파일이다. macOS가 exFAT 외장하드에 자동 생성하며,
  expo-router가 `app/**/._화면.tsx`를 라우트로 오인해 빌드가 깨진다.
  이때 브라우저에는 변경이 전달되지 않아 "새로고침이 안 되는" 것처럼 보인다.
- 이미 `metro.config.js`의 `resolver.blockList`가 차단하고 있다. 이 설정을 지우지 말 것.
- `start.sh`는 실행 중 3초마다 `._*` 파일을 자동 삭제한다.
- 그래도 안 되면 아래를 실행하고 서버를 재시작한다.
  ```bash
  find . -name '._*' -type f -delete
  ```
- `CI=1` 환경변수가 설정되어 있으면 Metro가 감시 모드를 끄므로 자동 새로고침이 동작하지 않는다.

## 이미지 제작 규칙 (필수 준수)

이 프로젝트에서 사용자가 "이미지 만들어보자", "이미지 만들어줘", "이미지 제작", "그림 생성" 등 단어나 개념에 대한 이미지 제작을 요청하는 경우, **기본적으로 모두 Draw Things 로컬 API 기반의 '선형그래픽(선화그래픽)' 생성 요청(`draw-things-linear-graphic` 스킬)**으로 간주하고 처리한다.

1. **Draw Things 설정**:
   - 로컬 API (`http://127.0.0.1:7860/sdapi/v1/txt2img`), Protocol: **HTTP**

2. **선형그래픽 조형 6대 원칙**:
   - `1024x1024` 네이티브 해상도, 0.05mm 초극세 바늘선 (`thinnest possible 0.05mm ultra-delicate needle-thin hairline ink stroke`)
   - 목 없는(No-Neck) 머리-몸통 직결 원형 머리 스틱맨 캐릭터
   - 뉴모피즘 캔버스 테마 `#f5f6f8` 배경 + `#030203` 딥 차콜 블랙 선
   - 풍성한 배경 씬(바닥선, 가구, 소품 등 내러티브 묘사)
   - 후처리 효과(필터, 임계값) 배제 원본 보존
   - 씬 내 텍스트는 영문(English)만 사용하며 한글 텍스트 전면 배제

3. **실시간 라이브 대시보드(Live Dashboard) 의무화**:
   - **단일 화면 실시간 리프레시 뷰 운영**: 채팅창에 긴 표를 반복 출력하면 메시지가 누적되어 화면이 지저분해지므로, 화면이 자동으로 갱신되는 전용 대시보드 2종을 반드시 생성 및 상시 업데이트한다.
     - `LIVE_DASHBOARD.md`: IDE 마크다운 미리보기(`Cmd+K V`)로 볼 수 있는 단일 상황판 덮어쓰기 유지.
     - `dashboard.html`: 8초 자동 새로고침 웹 대시보드로 현재 진행률, 소요시간, 진행 단어 실시간 갱신.
   - **채팅창 출력 최소화**: 2분 주기 알림 시 채팅창에는 긴 표 대신 **간결한 1줄 진행 상태 뱃지**만 출력하여 가독성을 유지한다.

4. **유닛 완료 시 즉시 앱 적용 및 Git 배포 의무화**:
   - 연속 생성 중이라도 **20단어 1개 유닛이 끝날 때마다 지체 없이 즉시 아래 파이프라인을 자동 수행**한다:
     1) `assets/words/<단어>.png` 이미지 저장
     2) `src/constants/wordImages.ts`에 단어 및 고유 ID(`m1-xxx`) 일괄 등록 (앱 즉시 반영)
     3) `npm run lint` 코드 검증 통과 확인
     4) 외장하드 임시파일 정리: `find . .. -name '._*' -type f -delete`
     5) 한국어 커밋 메시지로 Git commit (`feat: 중1 유닛 X 선형그래픽 일러스트 20단어 전원 생성 및 등록 완료`)
     6) 원격 저장소 푸시: `git pull --rebase origin main && git push origin main`
     7) 배포 완료 시점에 전체 집계 완료 보고를 채팅창에 출력한다.
