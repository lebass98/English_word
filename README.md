# WordPic — 이미지로 외우는 영어 단어

초6·중1·중2·중3 영어 단어를 연상 이미지와 함께 암기하는 앱.
Expo(React Native) + NativeWind로 모바일 앱 · 모바일 웹 · 반응형 태블릿 웹을 하나의 코드베이스로 지원합니다.

## 실행

```bash
npm run dev      # 웹 자동 새로고침 개발 서버 (브라우저 자동 오픈)
npm start        # Expo 개발 서버 (iOS/Android)
npm run web      # 웹 전용 실행
npm run lint     # ESLint
```

- **Windows 원클릭 실행**: `start.bat` 더블 클릭 (브라우저 자동 오픈 + 코드 저장 시 자동 새로고침)
- **macOS / Linux 실행**: `./start.sh` 실행 (브라우저 자동 오픈 + 코드 저장 시 자동 새로고침)

## 구조

- `app/` — expo-router 라우트 (메인 / 학년별 유닛 / 단어 학습)
- `src/components/` — 공용 컴포넌트 (PillButton, GradeCard, StatCard)
- `src/constants/` — 학년·단어 데이터 로더, 단어 이미지 레지스트리
- `src/data/` — 단어 데이터 (중1 994단어, 중2 1003단어)
- `assets/words/` — 단어별 연상 이미지 (`<단어id>.png`)

## 디자인 규칙

- 전체 UI는 **뉴모피즘** 유지 (버튼 포함). 토큰: `bg-canvas`/`bg-surface`, `shadow-neu`/`shadow-neu-sm`/`shadow-neu-inset`/`shadow-neu-pressed`
- 폰트 크기: 테마 레벨에서 기본 크기를 약 30% 축소하여 컴팩트하고 쾌적한 가독성 확보

## 작업 내역

### 2026-09-12
- 설정에 '발음 소리 크기' 추가 (음소거·25·50·75·100%). 슬라이더 라이브러리를 새로 들이지 않고 뉴모피즘 막대 단계로 만들었고, 단계를 누르면 바뀐 크기로 학습 언어의 짧은 견본을 바로 들려준다
- 학습 화면 스피커와 단어 자동 발음이 이 설정을 따른다. 음소거면 음성 엔진을 아예 깨우지 않는다
- 학습 화면 하단 버튼 줄 맨 왼쪽에 홈 버튼 추가 (글자 없이 아이콘만). 유닛을 거쳐 들어온 화면이라 뒤로가기만으로는 홈까지 여러 번 눌러야 했다
- 홈 버튼은 높이를 옆 버튼과 같게 늘리고 aspect-square 로 너비를 높이에 맞춰 정원으로 만든다 (크기를 손으로 적지 않아도 행 높이를 따라간다)
- 자리를 만들려고 저장 버튼은 flex-1 을 떼고 글씨('저장됨')에 맞춘 72px 로 줄이고, 판정 버튼 안쪽 아이콘 칸도 24px → 20px 로 줄임
- 하단 독 메뉴를 아이콘 왼쪽·제목 오른쪽 가로 배치로 바꾸고 높이를 학습 화면 상단 버튼과 같은 48px 로 맞춤
- 학습 화면 '헷갈려요'와 '외웠어요' 사이 가운데에 '저장' 버튼 추가. 누르면 단어장에 담기고 다시 누르면 빠진다 (눌린 상태는 안쪽 그림자와 채워진 북마크로 표시)
- 하단 세 버튼(헷갈려요·저장·외웠어요)을 똑같이 flex-1 로 나눠 폭을 같게 하고 좌우 여백 4px, 글씨 14px 로 통일. 가운데 저장 버튼만 고정폭이라 크기도 여백도 제각각으로 보이던 것을 정리
- 저장 버튼도 같은 몫을 가지므로 담겨서 '저장'→'저장됨' 으로 글자가 늘어도 버튼 폭이 흔들리지 않는다
- 일본어 '保存済み' 는 네 글자라 그 폭에 안 맞아 '保存済' 으로 줄임
- 판정 버튼 문구를 줄임: '아직 헷갈려요' → '헷갈려요', '외웠어요!' → '외웠어요' (일본어도 'まだあいまい' → 'あいまい', '覚えた！' → '覚えた')
- 단어장에 '저장한 단어' 분류 추가. 담은 시각 순으로 보여주며, '전체'에서는 판정한 단어와 담아 둔 단어를 섞어 최근 순으로 정렬
- 담기는 외움/헷갈림 판정과 별개다. 판정 없이 담기만 한 단어는 별이 비어 있다
- 단어장 빈 화면 안내가 번역을 거치지 않아 'wordbook.noKnown' 같은 키가 그대로 보이던 버그 수정
- 고1·고2·고3 일본어 번역 완료. 뜻 3,600개, 예문 해석·어원 각 2,168개, 유의어 뜻 5,893개 (합계 약 13,800항목)
- 고등 낱말 2,520개 중 352개는 중1·중2 번역을 그대로 쓰고 나머지 2,168개만 새로 옮김
- 문체는 기존 중1·중2 데이터에 맞춤: 뜻은 사전식에 전각 물결표(〜), 예문 해석은 반말(だ/である체), 어원은 존댓말(です・ます체)에 어근은 「」 뜻은 （）
- 어원의 유보 표현을 그대로 옮김 ("분명하지 않습니다" → 「はっきりしていません」). 불확실한 어원이 일본어판에서 단정으로 바뀌지 않게 함
- 원본 PDF 에서 글자가 빠진 뜻 5개 추가 수정: vacuum·twist·milestone 은 구분자 누락, consensus 는 쉼표 자리에 마침표, document 는 '기 록하다' 로 낱말 가운데가 벌어져 있었음
- 고등 2·3학년 각 1,200단어 추가. 이로써 중1~고3 6단계 6,797칸이 발음기호·품사·예문·예문 해석·어원·유의어를 모두 갖춤
- 2,400단어 중 1,266개는 이미 있는 낱말이라 그대로 쓰고, 새 낱말 1,134개만 새로 만듦
- 고3 PDF 에서 표 칸 안 줄바꿈으로 쪼개져 있던 counterproductive 복원
- 원본 PDF 에서 글자가 빠진 채 들어온 뜻 13개 수정. 낱말이 잘린 것(proof '증거, 증 견디는' → '증거, 증명; 견디는')과
  품사 사이 세미콜론이 안 찍힌 것(military '군사적인, 무력의 군대' → '…무력의; 군대') 두 갈래이며, 그중 8개는 이미 올린 고1 것
- 고등 1학년 1,200단어 추가. 발음기호·품사·예문·예문 해석·어원·유의어를 모두 갖춤
- 그중 732개는 중1·중2·중3에 이미 있는 낱말이라 그림과 예문을 그대로 쓰고, 새 낱말 468개만 새로 만듦
- 뜻은 단어 id 기준이라 present 처럼 중학교에도 나오는 낱말이 고1에서 다른 뜻이어도 섞이지 않는다
- 발음기호는 CMU 발음사전으로 1,199개 자동 생성, 사전에 없는 eco-friendly 만 손으로 넣음
- 품사 코드 aux(조동사) 이름이 i18n 에 없어 화면에 'pos.aux' 가 그대로 나오던 문제 수정
- 학습 화면 상단 영어 단어 최대 크기를 24px → 31px 로 키웠다가 다시 25px 로 20% 줄임
- 메인 '내 코스'를 한 줄에 두 개씩 배치. 카드도 좁은 폭에 맞춰 여백·글자를 줄이고 진행 막대와 개수를 위아래로 나눔
- 연상 이미지가 안 보이던 문제 수정: 그림을 `m1-1` 같은 단어 번호로 등록해 뒀는데 화면은 conceptId 와 철자로만 찾아서, 등록된 954개 중 절반이 사문이었다. 철자 기준으로 다시 등록해 그림 있는 낱말이 424개 → 578개
- `scripts/sync_word_images.py` 추가. 그림을 넣은 뒤 돌리면 레지스트리를 다시 만들어 준다 (등록 누락으로 그림이 사라지는 일을 막는다)
- 중3 1,200단어 어원 932개, 유의어 1,066단어분(약 2,800개) 생성. 중1·중2에 비어 있던 유의어 138단어도 함께 채움
- 중1 데이터 구멍 7개 보수: German·British·French 가 중1은 대문자, 중2는 소문자로 적혀 있어 중1 쪽이 비어 있던 문제 수정 (개념을 같게 두어 그림 공유), dining room 신규 추가, honour·hastily·unification 발음기호와 예문 추가
- 이로써 영어 3,197단어 전부가 발음기호·품사·예문·예문 해석·어원·유의어를 모두 갖춤
- 학습 언어 선택을 설정에서 메인 화면 상단으로 이동. 무엇을 배울지가 화면의 출발점이라 맨 위에 둠
- 국기를 SVG로 새로 그려 언어 버튼에 표시 (미국·일본). 그림 파일이 아니라 벡터라 확대해도 흐려지지 않는다
- 품사 표시 추가: 영어 1,007개 + 일본어 556개 단어에 품사 부여, 학습 화면에서 발음기호 옆에 표시
- 품사는 코드로 저장하고 이름은 표시 언어를 따른다 (명사/名詞). 일본어는 い형용사·な형용사를 구분
- 표시 언어 축 복원: 화면 말(한국어/일본어)과 학습 언어(영어/일본어)를 다시 분리. 일본인이 일본어 화면으로 중등 영어를 배울 수 있다
- 영어 단어의 일본어 어원 1,011개와 유의어 뜻 2,571개 신규 번역 (뜻·예문 해석은 git 이력에서 복원)
- 설정 화면에 아이콘 도입: 이름·언어·학습 설정·학습 기록 칸마다 제목 앞에 아이콘 배치
- 일본어 학습 언어 개통: JLPT N5 단어 556개 추가 (표기·히라가나 읽기·한국어 뜻·예문·예문 해석)
- 그중 153개는 기존 영어 단어의 그림과 같은 뜻이라 그림을 그대로 재사용 (28%)
- 학습 화면이 설정이 아니라 단어 id 에 적힌 언어를 따르도록 변경. 단계 이름과 발음 언어도 같은 기준
- 이미지 제작 요청 기본 규칙 확립: 프로젝트 내 모든 이미지 제작 요청을 Draw Things 로컬 API 기반 '선형그래픽(draw-things-linear-graphic)' 표준으로 일괄 처리하도록 규칙(AGENTS.md, CLAUDE.md, .agents/rules/) 등록
- 중1 Unit 6 (grain ~ sight, m1-101 ~ m1-120) 20단어 선형그래픽 일러스트 100% 제작 및 등록 배포
- 중1 Unit 5 (course ~ hometown, m1-81 ~ m1-100) 20단어 선형그래픽 일러스트 100% 제작 및 등록 배포
- 중1 Unit 4 잔여 6단어 (effect ~ stadium, m1-75 ~ m1-80) 선형그래픽 일러스트 제작 및 등록 배포 (Unit 4 100% 완료)
- 방향 재정립: 사용자는 항상 한국인. 화면 말은 한국어 하나로 고정하고 표시 언어 설정을 제거
- 설정의 언어 선택을 "학습 언어"(영어 / 일본어)로 변경
- 연상 이미지를 낱말이 아니라 뜻(개념)에 붙이도록 conceptId 도입. 언어가 달라도 같은 개념이면 그림 한 장을 함께 쓴다
- 표시 언어용으로 만들었던 영어 단어의 일본어 뜻·예문(en/tr/ja.json) 제거 (git 이력 ce3cc2a 에 남아 있음)
- 일본어 표시 언어 콘텐츠 채움: 영어 단어 뜻 1,997개와 예문 해석 1,003개를 일본어로 번역 (어원·유의어 뜻은 아직 한국어로 대체 중)
- 일본어 글꼴 Noto Sans JP 적용. 한글은 이 글꼴에 없어 뒤쪽 한글 글꼴로 자동으로 넘어간다
- 번역 합치기 수정: 상세 정보를 항목째가 아니라 칸 단위로 채워, 일부만 번역된 단어에서 나머지 칸이 사라지지 않게 함
- 다국어 기반 구조 도입: 표시 언어(앱 화면의 말)와 학습 언어(배우는 말)를 따로 고를 수 있게 분리
- 단어 데이터를 언어별로 재구성 (`src/data/en/`): 학습 언어 자체의 정보(발음기호·예문·유의어 철자)와 표시 언어별 번역(뜻·예문 해석·어원)을 분리, 원본과 완전 일치 검증
- 화면 문구 94개를 `src/i18n/`로 옮기고 일본어 번역 추가. 빠진 문구는 한국어로 자동 대체
- 단어 id에 학습 언어 접두사 부여(`m1-1` → `en-m1-1`), 기존 학습 기록은 저장소 마이그레이션으로 이전
- 설정 화면에 표시 언어·학습 언어 선택 추가, 발음 재생이 학습 언어를 따르도록 연결

### 2026-09-09
- Expo + TypeScript + NativeWind(Tailwind) 초기 셋팅, expo-router로 앱/웹 단일 코드베이스 구성
- TanStack Query·Zustand·스토리지 추상화 등 기본 뼈대 추가
- 메인 화면: 헤더·프로필·탭 칩·성취 기록 2×2·학년 선택 카드·하단 네비
- 뉴모피즘 디자인 시스템 확립 (카드·칩·버튼·inset 토큰화), 필 버튼 공용 컴포넌트
- 단어 학습 화면: 플래시카드(뜻 가리기/북마크/거꾸로 학습 토글), 유닛 진행률
- 중학 1학년 단어 994개 데이터 입력(50유닛), 이미지 자리는 placeholder로 비움
- 중학 1학년 1단원 20개 단어 연상 암기 이미지 생성 및 등록
- 학습 학년을 중1~3, 고1~3으로 변경 및 ._ 파일 자동 삭제 훅 추가
- 중학 2학년 단어 1003개 데이터 추가(DAY 01~34, 51개 유닛 분할)
- 중1 1단원 20개 연상 이미지 상단 텍스트(단어, 노란 박스, 별점, 뜻) 제거 및 순수 일러스트 클린업
- 전체 UI 글씨 크기 30% 축소 적용 (tailwind theme.extend.fontSize 및 화면별 위계 정리)

### 2026-09-10
- 학습 화면 `가리기` 버튼이 예문뿐 아니라 한글 뜻까지 함께 가리도록 변경 (가려진 자리를 누르면 바로 표시)
- 이미지 좌우 스와이프로 앞뒤 단어 이동 추가 (그림만 캐러셀처럼 흐르고 단어·발음 영역은 고정)

### 2026-09-11
- 중학교 2학년 Unit 47~51 기존 단어 이미지 83개를 새 연상 일러스트로 전면 교체
- 중학교 2학년 Unit 17·18 단어 40개 연상 이미지 생성 및 학습 카드 매핑 등록
- 중학교 2학년 Unit 16 동사 20개(yell~drag) 연상 이미지 생성 및 학습 카드 매핑 등록
- Draw Things 로컬 API(127.0.0.1:7860) 연결 및 단어 이미지 자동 생성/매핑 스크립트 개선
- 휴가 신청 UI용 SVG 벡터 아이콘 4종 추가 (assets/icons/)
- Draw Things + potrace 연동 AI 생성 기반 초경량 SVG 벡터 변환 파이프라인 구축 (generate_svg_with_drawthings.py)
- 작업 완료 시 README 일별 작업 내역 자동 갱신 및 한글 커밋/푸시/풀 규칙 확립
- 중학교 2학년 Unit 15 (20개 단어: discuss, shrug, sniff 등) Draw Things 기반 연상 이미지 18종 생성 및 wordImages.ts 매핑 등록
- 중2 Unit 15 discuss(토론하다) 연상 이미지 초극세 펜선 및 회의실 씬으로 고도화 재생성
- 선형그래픽 표준 셋팅 확립: 초기 초극세선 셋팅, 배경 #f5f6f8, 선색 #030203, 풍성한 씬 구성 및 후처리 효과 없는 순수 출력 적용 (discuss 반영)
- Draw Things 선형그래픽 전용 에이전트 스킬(draw-things-linear-graphic) 구축 및 저장소 내장 (어느 컴퓨터에서든 git clone/pull 시 즉시 로드)
- Draw Things 로컬 API 연결 검증 및 Node.js 기반 선형그래픽 생성 엔진(generate_linear_graphic.js) 추가, 테스트 이미지(connect.png) 생성 완료
- 중학교 2학년 Unit 15 (2번~20번 단어 19종: shrug~prove) 선형그래픽 스킬 규격(0.05mm 초극세선, 1024x1024, 배경 #f5f6f8, 선색 #030203, 풍성한 씬)으로 전면 재생성 및 교체 적용
- GitHub Actions CI/Deploy 실패 수정: wordImages.ts 내 8개 단어 중복 프로퍼티 키 제거로 TS1117 타입 에러 해결 및 빌드 검증
- Draw Things 선형그래픽 스킬 업데이트: 인물/스틱맨 생성 시 목 없이 머리가 몸통에 바로 연결되는 노넥(No-Neck) 캐릭터 조형 규칙 공식 반영
- 중학교 1학년 Unit 1 (20개 단어 전원: beyond~teenager) 선형그래픽 스킬 규격(0.05mm 초극세선, 1024x1024, 배경 #f5f6f8, 선색 #030203, 노넥 스틱맨, 풍성한 씬)으로 전면 재생성 및 교체 적용
- 배치 파일(start.bat / start.sh)에 자동 새로고침(Fast Refresh) 로컬 웹 서버 실행 및 브라우저 자동 오픈 연동
- package.json 내 npm run dev 개발 서버 스크립트 추가 및 서버 종료 시 비정상 튕김 방지 안내 처리
- 자동 넘김(재생/멈춤) 설정을 zustand persist(AsyncStorage)로 영구 저장 — 단어 이동·뒤로가기·앱 재시작에도 유지
- 메인 화면을 실데이터 기반 "이어하기 홈"으로 전면 재작성 — 가짜 성취기록·프로필·출석뱃지·동작 안 하던 탭/벨 제거
- 학습 기록 영구 저장(zustand persist): entries·lastStudied·dailyLog·nickname, 일별 기록 60일 보관
- 홈 구성: 스트릭 칩 · 이어하기 히어로 카드 · 헷갈리는 단어 복습 · 오늘의 기록 3종 · 내 코스(학습률 실값)
- 단어장(app/wordbook.tsx)·설정(app/settings.tsx) 화면 신설, 하단 네비 3탭 실동작(usePathname 기반)
- 유닛 목록의 "0 / 20"을 실제 외운 개수로 교체하고 유닛별 진행바·완료 표시 추가
- findWord O(1) 색인화 및 unitsOf 캐시로 목록 성능 개선
- SSR(Node) 환경에서 AsyncStorage가 window를 참조해 개발 서버가 죽던 문제 수정(persistStorage 어댑터)
- Draw Things 선형그래픽 스킬 업데이트: 한글 텍스트 전면 배제 및 영문 우선 표기 원칙(원칙 7) 공식화, 100% 영문 프롬프트 구성 및 네거티브 프롬프트 한글 차단 반영
- 중학교 1학년 Unit 2 (20개 단어 전원: sophomore~brown) 선형그래픽 스킬 규격(0.05mm 초극세선, 1024x1024, 배경 #f5f6f8, 선색 #030203, 노넥 스틱맨, 영문 연출)으로 일괄 생성 및 wordImages.ts 매핑 등록
- 중학교 1학년 Unit 3 (20개 단어 전원: decrease~nearly) 선형그래픽 스킬 규격으로 일괄 생성 및 wordImages.ts 매핑 등록, TS1117 중복 키 방지 및 타입체크/웹 빌드 검증 완료
- Draw Things 선형그래픽 스킬 업데이트: 다중 이미지 일괄 생성 시 실시간 진행 상황판(Dashboard Table) 안내 및 2분 주기 갱신 규칙 명문화
- 단어 학습 카드에 유의어·어원 섹션 추가, 예문까지 알약 라벨 형태로 통일(LabeledSection) 및 좌측 정렬
- 전체 고유 단어 1,011개의 유의어·어원 데이터 생성 및 wordDetails.json 병합(어원 1,011 / 유의어 873, 기능어 138개는 유의어 없음)
- 유의어에 문맥에 맞는 한글 뜻 병기(2,571개 생성) — Word.synonyms를 {word, meaning} 구조로 변경, SynonymList 컴포넌트 분리
- 중학교 1학년 Unit 3 (20개 단어 전원: decrease~nearly) 선형그래픽 스킬 규격(1024x1024, 0.05mm 초극세선, 노넥 캐릭터, 배경 #f5f6f8, 선색 #030203, 영문 연출)으로 전원 신규 생성 및 교체 완료
- 중학교 1학년 Unit 4 단어 이미지 선형그래픽 스킬 기반 14종(almost~sample) 1024x1024 신규 생성 완료 및 wordImages.ts 매핑 등록 (잔여 6종 재개 CLI 옵션 지원)

